# Changelog

## v0.3.1 — README 联动链接修正（2026-08-19）

### 🔧 修复

- 修正「与助手管理插件联动」章节的链接：指向小花记忆真实仓库（DeepSeek-Harness-Hanako-Memory），之前误写了不存在的仓库名

## v0.3.0 — 每位助手单独设置（per-preset）（2026-08-18）

### ✨ 新功能

- **每位助手单独设置**：设置页新增「每位助手单独设置」区块，自动列出 dsh 里已创建的助手预设，逐个配置：配图开关、日常/正事配图频率、方言口音（含加强版）。保存后只影响该助手，其他助手不变
- **per-agent 方言**：方言注入按当前会话的助手动态渲染（通过会话的 preset 身份解析），每个助手可以有自己的家乡味，互相不串
- **per-agent 配图频率与开关**：观察器自动配图按当前助手的频率工作；某助手关闭后，聊天中 Ta 不会配图，其他助手不受影响
- **per-agent 偏好隔离**：每个助手的喜欢/不喜欢反馈独立记录，没单独调教过的助手继承全局偏好
- **内容分析模型独立配置**：设置页新增「内容分析模型」区块，聊天时自动配图（观察器）与「学我说话」的风格分析用独立模型。建议选速度快、价格低的模型；不配置时自动回退用识图模型，旧配置零影响
- **自定义 API 完整支持**：识图模型与内容分析模型都支持自定义 OpenAI 兼容端点：填 Base URL + API Key →「读取模型列表」从你的服务拉取可用模型 → 下拉选择 → 保存后识图/内容分析直接走这个自定义 API（无需 dsh 预配 provider）。Key 本地脱敏存储，占位符不覆盖已存值

### ⚙️ 兼容与默认

- 全局设置（默认配图频率 / 默认方言）保留为「默认值」：没单独设置过的助手自动沿用，老配置零迁移
- 设置页「默认配图频率」「默认方言口音」区块标注了「默认」语义
- **不依赖任何其他插件**：没有助手预设（未装助手管理类插件）时，全局默认设置直接生效，功能与旧版一致；有助手预设时 per-agent 设置自动可用

### 🔧 修复

- 配图频率「不配图」（0 档）在设置页显示异常的问题（0 被误显示为默认值）
- 观察器多会话并发时提示串台：提示注入现在按会话核对，冷却也按助手隔离
- 无效方言 id 写入后残留空配置条目：现在会自动清理
- **设置页操作反馈不可见**：保存/测试按钮的提示原本渲染在长页面底部，看不到就像「点了没反应」。现在提示吸顶显示（sticky）并 3.5 秒自动消失
- **测试按钮语义**：「测试一下」测的是**表单当前值**（自定义 API 填了什么就测什么、下拉选了谁就测谁），**不用先保存**——先测试、通了再保存，符合直觉。没有表单输入时才回退到已保存配置，都没配则提示未配置
- **推理级别冲突**：模型调用显式指定推理级别 off（轻任务不需要思考），不再继承 dsh provider 默认推理级别——DeepSeek 官方直连对 flash 默认 max 会直接报「不支持推理级别 max」，现在所有调用（识图/内容分析/观察器/学我说话/和助手聊聊/连通测试）都绕开了这个坑
- **测试未配置友好提示**：模型区块没配置时点「测试一下」直接提示「未配置内容分析/识图模型」，不再悄悄 fallback 到默认模型报出莫名的模型错误

### 🧪 测试

- 新增会话→助手解析（header / 切换事件 / 兜底）与 per-preset 配置合并（无覆盖 / 部分覆盖 / 全局关闭）共 7 个用例，46 个测试全绿
- 端到端实测（本地 mock OpenAI 服务）：自定义 API 读取模型列表、配置保存、连通测试、占位符 Key 回退全链路通过；设置页按钮反馈吸顶可见
## v0.2.1 — 修复抽屉层级（2026-08-16）

### 🔧 修复

- **修复表情包抽屉被右侧栏遮挡**：抽屉右侧约 260px 会藏在 Explorer 列后面（shell.overlay 是独立层叠上下文，抽屉的 z-index 被封装在层内，永远盖不过右侧栏）。现在抽屉改用 ReactDOM.createPortal 挂到 document.body，z-index 900/901 直接活在根层叠上下文里，正确盖过右侧栏，同时不影响 1000+ 的弹层

### 🙏 感谢

- 这个修复来自外部贡献者 **@lpreterite** 的 PR #1：不仅定位到层叠上下文根源，还附带了 38 项测试全绿验证和 4 张 before/after 截图证据，顺手在 dsh 官方仓库提了配套修复（dsh-web-ui PR #259）。这是项目收到的第一个外部 PR，对我们意义很特别！

## v0.2.0 — 和助手聊聊 + 反馈体验升级（2026-08-15）

### ✨ 新功能

- **和助手聊聊**：配图不满意？点「不喜欢」后卡片上会出现聊聊入口，直接和助手对话调教这张图的标签。多轮聊天，达成共识后助手会给出修改建议，一键应用就写进图库
- **设置页新增「最近配图记录」**：每次配图都有记录（缩略图 + 时间 + 情绪），随时点「和助手聊聊」回头调教
- **「显示反馈按钮」开关**：不想要聊天卡片上的喜欢/不喜欢按钮，可以在设置里关掉，卡片就只剩一张干干净净的图

### 🎯 反馈体验

- 配图卡片只显示图片本体，不再有图片名称、匹配度这些文字
- 喜欢/不喜欢按钮居中显示
- 反馈状态持久显示：点过的图再次出现时按钮直接亮灯，一看就知道之前的态度；每一轮仍然可以重新表达（切换方向或继续累计）

### 🔧 修复

- 修复「和助手聊聊」模型调用失败（消息 content 需数组格式、历史 assistant 消息需携带 source）
- 修复方言加强版注入消息角色不合法导致 dsh 会话历史损坏、重启加载失败的问题
